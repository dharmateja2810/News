/**
 * E2E Test Setup
 * Global configuration and utilities for end-to-end tests
 */

// Increase Jest timeout for E2E tests
jest.setTimeout(30000);

// Suppress console logs during tests (optional)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
// };
